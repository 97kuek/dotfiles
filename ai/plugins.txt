# ClaudeとCodexに入れる外部プラグイン。install.shが未導入のものだけを導入する。
#
# 形式: <プラグイン>@<マーケットプレイス>  [対象CLI]
#   対象CLI … claude / codex をカンマ区切り。省略すると両方。
#
# プラグインはCLIの設定ディレクトリに入るため、CLIごと・アカウントごとに導入される。
# 複数のスキルやフック、LSPをまとめて配っているものをここに書く。
# スキルを1つだけ入れたいときは skills.txt、自作スキルは ai/skills/ に置く。
#
# 中身はこのリポジトリに置かない。配布元が更新すれば
# `claude plugin update` / `codex plugin marketplace upgrade` で新しくなる。
#
# #から行末まではコメント。空行は無視される。

mattpocock-skills@claude-plugins-official          # grill-me、grilling、tddなど
clangd-lsp@claude-plugins-official                 # C/C++のLSP
codex@openai-codex                                 # Claude CodeからCodexを呼ぶ。Codexにも元から入っている

# 設計→計画→実装→レビューの進め方。
# Codexの openai-curated-remote にもあるが、ChatGPTにログインしていないアカウントでは入れられない。
superpowers@claude-plugins-official
